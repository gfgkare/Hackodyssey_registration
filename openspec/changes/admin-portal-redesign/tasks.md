# Tasks

## 1. Admin Dashboard Redesign

- [x] 1.1 Redesign the header layout and controls in `AdminDashboard.tsx` to match the sci-fi Mission Control aesthetic, including integrated tab toggle, search, filter dropdowns, refresh button, export buttons, and logout button, and verify visually in browser.
- [x] 1.2 Redesign the TEAMS table in `AdminDashboard.tsx` to display all production columns (`#`, `TEAM NAME` with ID subtitle, `CONTACT INTEL` with leader user/email/phone icons, `UNIT SIZE` circular badge, `CATEGORY`, and `STATUS`), and verify layout responsiveness.
- [x] 1.3 Redesign the PARTICIPANTS table in `AdminDashboard.tsx` to display all production columns (`#`, `TEAM NAME`, `NAME`, `REG NO`, `GENDER`, `YEAR/BRANCH`, `PHONE`, `EMAIL`, `COLLEGE`, `ACCOMMODATION` with styled badges, and `HOSTEL DETAILS`), and verify filters for gender and accommodation.

## 2. Admin Login Redesign

- [x] 2.1 Overhaul `AdminLogin.tsx` to match the dark futuristic cyber command theme with dark card, subtle borders, glowing accent lights, and themed inputs, and verify login behavior.

## 3. Verification & Build Validation

- [x] 3.1 Run frontend TypeScript build (`npm run build` in `frontend/`) to ensure zero type errors or regressions.
